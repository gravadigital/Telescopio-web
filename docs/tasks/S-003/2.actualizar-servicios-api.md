# Task S-003.2: Actualizar Servicios API

## Status

**Completed**

## Description

Modificar el método `updateEventStage` en `EventService` para aceptar un parámetro opcional `estimatedEndDate`, y agregar un nuevo método `updateEstimatedEndDate` para editar fechas estimativas existentes.

**Conexión con Story**: Esta tarea implementa la comunicación con los endpoints del backend definidos en la story S-003.

## Acceptance Criteria

1. El método `EventService.updateEventStage` acepta un parámetro opcional `estimatedEndDate?: string`.

2. Si se proporciona `estimatedEndDate`, se incluye en el body del request como `estimated_end_date`.

3. Se crea un nuevo método `EventService.updateEstimatedEndDate(eventId, stage, estimatedEndDate)`.

4. El nuevo método hace `PATCH` al endpoint `/api/v1/events/{id}/estimated-end-date`.

5. Ambos métodos manejan errores correctamente y los propagan al llamador.

6. El código compila sin errores de TypeScript.

## Technical Details

**Archivos a Modificar:**
- `Telescopio-web/src/services/api.ts`

**Implementación:**

### 1. Modificar updateEventStage

**ANTES:**
```typescript
async updateEventStage(eventId: string, newStage: string): Promise<void> {
  try {
    await apiRequest<any>(
      API_CONFIG.ENDPOINTS.EVENT_STAGE(eventId),
      {
        method: "PATCH",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({ stage: newStage }),
      }
    );
  } catch (error) {
    console.error("Failed to update event stage:", error);
    throw error;
  }
}
```

**DESPUÉS:**
```typescript
async updateEventStage(
  eventId: string, 
  newStage: string, 
  estimatedEndDate?: string
): Promise<Event> {
  try {
    const body: { stage: string; estimated_end_date?: string } = { 
      stage: newStage 
    };
    
    if (estimatedEndDate) {
      body.estimated_end_date = estimatedEndDate;
    }

    const response = await apiRequest<{ data: any }>(
      API_CONFIG.ENDPOINTS.EVENT_STAGE(eventId),
      {
        method: "PATCH",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(body),
      }
    );

    // Map response to Event type
    const event = response.data;
    return {
      id: event.id,
      title: event.name || event.title,
      description: event.description,
      date: event.start_date || event.date,
      stage: event.stage,
      participation_estimated_end_date: event.participation_estimated_end_date,
      voting_estimated_end_date: event.voting_estimated_end_date,
      creator_id: event.author_id,
      updated_at: event.updated_at
    } as Event;
  } catch (error) {
    console.error("Failed to update event stage:", error);
    throw error;
  }
}
```

### 2. Agregar método updateEstimatedEndDate

```typescript
/**
 * Update estimated end date for a specific stage
 * @param eventId - Event ID
 * @param stage - 'participation' or 'voting'
 * @param estimatedEndDate - New date in YYYY-MM-DD format
 */
async updateEstimatedEndDate(
  eventId: string,
  stage: 'participation' | 'voting',
  estimatedEndDate: string
): Promise<{ previousDate: string | null; newDate: string }> {
  try {
    console.log('📅 Updating estimated end date:', {
      eventId,
      stage,
      estimatedEndDate
    });

    const response = await apiRequest<{
      data: {
        event_id: string;
        stage: string;
        estimated_end_date: string;
        previous_date: string;
      };
      message: string;
      code: string;
    }>(
      API_CONFIG.ENDPOINTS.EVENT_ESTIMATED_DATE(eventId),
      {
        method: "PATCH",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          stage,
          estimated_end_date: estimatedEndDate
        }),
      }
    );

    console.log('✅ Estimated end date updated:', response.data);
    
    return {
      previousDate: response.data.previous_date || null,
      newDate: response.data.estimated_end_date
    };
  } catch (error) {
    console.error("❌ Failed to update estimated end date:", error);
    throw error;
  }
}
```

### 3. Actualizar getEventById para incluir fechas estimativas

Asegurar que el mapping incluya los nuevos campos:

```typescript
async getEventById(id: string): Promise<Event | null> {
  try {
    const response = await apiRequest<{ data: any }>(`${API_CONFIG.ENDPOINTS.EVENTS}/${id}`);
    
    if (!response || !response.data) {
      throw new Error("No response from API");
    }

    const event = response.data;
    console.log("✅ Event details loaded from backend:", event.id);

    return {
      id: event.id,
      title: event.name || event.title,
      description: event.description,
      date: event.start_date || event.date,
      organizer: event.organizer || "Organizador por determinar",
      status: event.status || "active",
      stage: event.stage || "participation",
      participant_ids: event.participant_ids || [],
      voteCount: event.vote_count || { yes: 0, maybe: 0, no: 0 },
      attachmentCount: event.attachment_count || 0,
      max_participants: event.max_participants,
      creator_id: event.author_id || event.creator_id,
      created_at: event.created_at,
      updated_at: event.updated_at,
      // Nuevos campos S-003
      participation_estimated_end_date: event.participation_estimated_end_date || null,
      voting_estimated_end_date: event.voting_estimated_end_date || null
    };
  } catch (error) {
    // ... manejo de error existente ...
  }
}
```

## Testing Requirements

**Manual Testing:**

1. **Verificar que updateEventStage funciona sin fecha:**
   ```typescript
   // En consola o test
   await EventService.updateEventStage(eventId, 'participation');
   // Debe fallar con error "MISSING_ESTIMATED_DATE" (backend requiere fecha)
   ```

2. **Verificar que updateEventStage funciona con fecha:**
   ```typescript
   const event = await EventService.updateEventStage(eventId, 'participation', '2026-02-15');
   console.log(event.participation_estimated_end_date);
   // Debe mostrar "2026-02-15"
   ```

3. **Verificar updateEstimatedEndDate:**
   ```typescript
   const result = await EventService.updateEstimatedEndDate(eventId, 'participation', '2026-02-20');
   console.log(result);
   // { previousDate: "2026-02-15", newDate: "2026-02-20" }
   ```

4. **Verificar getEventById retorna fechas:**
   ```typescript
   const event = await EventService.getEventById(eventId);
   console.log(event?.participation_estimated_end_date);
   // Debe mostrar la fecha si existe
   ```

## Task Dependencies

**Blocked By:**
- Task S-003.1 (Actualizar Tipos) - Necesita la interface Event actualizada

**Blocks:**
- Task S-003.3 (Crear StageAdvanceModal) - Necesita los métodos del servicio
- Task S-003.4 (Integrar Modal) - Necesita los métodos del servicio
- Task S-003.5 (Mostrar en EventDetail) - Necesita el mapping correcto en getEventById

**Can Run In Parallel:**
- Ninguna - Las tareas 3, 4 y 5 dependen de esta

