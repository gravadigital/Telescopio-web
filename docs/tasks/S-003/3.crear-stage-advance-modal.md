# Task S-003.3: Crear Componente StageAdvanceModal

## Status

**Completed**

## Description

Crear un nuevo componente modal reutilizable que se muestra cuando el organizador quiere avanzar de etapa. El modal muestra un mensaje de confirmación y, para las etapas `participation` y `voting`, incluye un campo de fecha obligatorio para definir la fecha estimativa de cierre.

**Conexión con Story**: Esta tarea implementa los criterios de aceptación CA1 (modal de confirmación), CA2 (campo de fecha) y CA3 (validación de fechas) de la story S-003.

## Acceptance Criteria

1. Se crea el componente `StageAdvanceModal` en `src/components/stage-advance-modal/`.

2. El modal muestra el título "Advance to [NextStage]?" con mensaje de confirmación.

3. Si `nextStage` es `participation` o `voting`, muestra un campo de fecha obligatorio.

4. La fecha tiene valores por defecto:
   - Para `participation`: HOY + 7 días
   - Para `voting`: HOY + 3 días

5. Validación: No se puede seleccionar fecha anterior a HOY (input min).

6. El botón "Confirm" está deshabilitado si falta la fecha requerida.

7. Props del componente:
   - `currentStage`: Etapa actual
   - `nextStage`: Etapa destino
   - `onConfirm`: Callback con fecha opcional
   - `onCancel`: Callback para cerrar
   - `isLoading`: Estado de carga

8. El componente incluye estilos CSS propios.

## Technical Details

**Archivos a Crear:**
- `Telescopio-web/src/components/stage-advance-modal/StageAdvanceModal.tsx`
- `Telescopio-web/src/components/stage-advance-modal/StageAdvanceModal.css`

**Implementación:**

### StageAdvanceModal.tsx

```tsx
import React, { useState, useMemo } from 'react';
import './StageAdvanceModal.css';

type EventStage = 'creation' | 'participation' | 'voting' | 'results';

interface StageAdvanceModalProps {
  currentStage: EventStage;
  nextStage: EventStage;
  onConfirm: (estimatedEndDate?: string) => Promise<void>;
  onCancel: () => void;
  isLoading?: boolean;
}

const STAGE_NAMES: Record<EventStage, string> = {
  creation: 'Creation',
  participation: 'Participation',
  voting: 'Voting',
  results: 'Results'
};

const StageAdvanceModal: React.FC<StageAdvanceModalProps> = ({
  currentStage,
  nextStage,
  onConfirm,
  onCancel,
  isLoading = false
}) => {
  // Calcular fecha por defecto según la etapa
  const defaultDate = useMemo(() => {
    const today = new Date();
    const daysToAdd = nextStage === 'participation' ? 7 : 3;
    today.setDate(today.getDate() + daysToAdd);
    return today.toISOString().split('T')[0];
  }, [nextStage]);

  const [estimatedDate, setEstimatedDate] = useState<string>(defaultDate);
  const [error, setError] = useState<string>('');

  // Determinar si esta etapa requiere fecha
  const requiresDate = nextStage === 'participation' || nextStage === 'voting';

  // Fecha mínima (hoy)
  const minDate = new Date().toISOString().split('T')[0];

  const handleConfirm = async () => {
    setError('');

    // Validar fecha si es requerida
    if (requiresDate) {
      if (!estimatedDate) {
        setError('Please select an estimated end date');
        return;
      }

      if (estimatedDate < minDate) {
        setError('Date cannot be in the past');
        return;
      }
    }

    try {
      await onConfirm(requiresDate ? estimatedDate : undefined);
    } catch (err: any) {
      setError(err.message || 'Failed to advance stage');
    }
  };

  const getStageDescription = (): string => {
    switch (nextStage) {
      case 'participation':
        return 'Participants will be able to register and upload their files.';
      case 'voting':
        return 'Participants will be able to vote on submitted entries.';
      case 'results':
        return 'Voting will close and results will be visible.';
      default:
        return '';
    }
  };

  return (
    <div className="stage-modal-overlay" onClick={onCancel}>
      <div className="stage-modal" onClick={e => e.stopPropagation()}>
        <div className="stage-modal-header">
          <h2>Advance to {STAGE_NAMES[nextStage]}?</h2>
          <button className="stage-modal-close" onClick={onCancel}>×</button>
        </div>

        <div className="stage-modal-body">
          <p className="stage-modal-description">
            {getStageDescription()}
          </p>

          <div className="stage-transition-info">
            <span className="stage-badge current">{STAGE_NAMES[currentStage]}</span>
            <span className="stage-arrow">→</span>
            <span className="stage-badge next">{STAGE_NAMES[nextStage]}</span>
          </div>

          {requiresDate && (
            <div className="stage-modal-date-field">
              <label htmlFor="estimated-end-date">
                📅 Estimated End Date for {STAGE_NAMES[nextStage]}
                <span className="required">*</span>
              </label>
              <p className="field-hint">
                This date will be shown to participants so they know when this stage is expected to close.
              </p>
              <input
                type="date"
                id="estimated-end-date"
                value={estimatedDate}
                onChange={(e) => setEstimatedDate(e.target.value)}
                min={minDate}
                disabled={isLoading}
              />
            </div>
          )}

          {error && (
            <div className="stage-modal-error">
              ⚠️ {error}
            </div>
          )}
        </div>

        <div className="stage-modal-footer">
          <button
            className="btn btn-secondary"
            onClick={onCancel}
            disabled={isLoading}
          >
            Cancel
          </button>
          <button
            className="btn btn-primary"
            onClick={handleConfirm}
            disabled={isLoading || (requiresDate && !estimatedDate)}
          >
            {isLoading ? 'Updating...' : 'Confirm'}
          </button>
        </div>
      </div>
    </div>
  );
};

export default StageAdvanceModal;
```

### StageAdvanceModal.css

```css
.stage-modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  animation: fadeIn 0.2s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.stage-modal {
  background: var(--surface-color, #1a1a2e);
  border-radius: 12px;
  width: 90%;
  max-width: 450px;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
  animation: slideUp 0.3s ease-out;
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.stage-modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 24px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.stage-modal-header h2 {
  margin: 0;
  font-size: 1.25rem;
  color: var(--text-primary, #fff);
}

.stage-modal-close {
  background: none;
  border: none;
  font-size: 1.5rem;
  color: var(--text-secondary, #888);
  cursor: pointer;
  padding: 0;
  line-height: 1;
}

.stage-modal-close:hover {
  color: var(--text-primary, #fff);
}

.stage-modal-body {
  padding: 24px;
}

.stage-modal-description {
  color: var(--text-secondary, #aaa);
  margin-bottom: 20px;
  line-height: 1.5;
}

.stage-transition-info {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  margin-bottom: 24px;
  padding: 16px;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 8px;
}

.stage-badge {
  padding: 6px 12px;
  border-radius: 6px;
  font-size: 0.875rem;
  font-weight: 500;
}

.stage-badge.current {
  background: rgba(255, 255, 255, 0.1);
  color: var(--text-secondary, #aaa);
}

.stage-badge.next {
  background: var(--primary-color, #6c5ce7);
  color: white;
}

.stage-arrow {
  color: var(--text-secondary, #888);
  font-size: 1.25rem;
}

.stage-modal-date-field {
  margin-top: 20px;
}

.stage-modal-date-field label {
  display: block;
  color: var(--text-primary, #fff);
  font-weight: 500;
  margin-bottom: 8px;
}

.stage-modal-date-field .required {
  color: #e74c3c;
  margin-left: 4px;
}

.stage-modal-date-field .field-hint {
  font-size: 0.85rem;
  color: var(--text-secondary, #888);
  margin-bottom: 12px;
}

.stage-modal-date-field input[type="date"] {
  width: 100%;
  padding: 12px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.05);
  color: var(--text-primary, #fff);
  font-size: 1rem;
}

.stage-modal-date-field input[type="date"]:focus {
  outline: none;
  border-color: var(--primary-color, #6c5ce7);
}

.stage-modal-error {
  margin-top: 16px;
  padding: 12px;
  background: rgba(231, 76, 60, 0.1);
  border: 1px solid rgba(231, 76, 60, 0.3);
  border-radius: 8px;
  color: #e74c3c;
  font-size: 0.9rem;
}

.stage-modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  padding: 16px 24px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.stage-modal-footer .btn {
  padding: 10px 20px;
  border-radius: 8px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s;
}

.stage-modal-footer .btn-secondary {
  background: transparent;
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: var(--text-secondary, #aaa);
}

.stage-modal-footer .btn-secondary:hover:not(:disabled) {
  background: rgba(255, 255, 255, 0.05);
}

.stage-modal-footer .btn-primary {
  background: var(--primary-color, #6c5ce7);
  border: none;
  color: white;
}

.stage-modal-footer .btn-primary:hover:not(:disabled) {
  background: var(--primary-hover, #5b4cdb);
}

.stage-modal-footer .btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
```

## Testing Requirements

**Manual Testing:**

1. **Renderizar modal para avanzar a participation:**
   - Debe mostrar campo de fecha
   - Fecha default: HOY + 7 días
   - Botón "Confirm" habilitado

2. **Renderizar modal para avanzar a voting:**
   - Debe mostrar campo de fecha
   - Fecha default: HOY + 3 días
   - Botón "Confirm" habilitado

3. **Renderizar modal para avanzar a results:**
   - NO debe mostrar campo de fecha
   - Botón "Confirm" habilitado

4. **Validación de fecha:**
   - Borrar la fecha → Botón "Confirm" deshabilitado
   - Intentar seleccionar fecha pasada → Input no lo permite (min attribute)

5. **Interacción:**
   - Click en overlay → Cierra modal (onCancel)
   - Click en botón X → Cierra modal (onCancel)
   - Click en "Cancel" → Cierra modal (onCancel)
   - Click en "Confirm" → Llama onConfirm con fecha (si aplica)

## Task Dependencies

**Blocked By:**
- Task S-003.2 (Actualizar Servicios API) - Para saber la interface del callback

**Blocks:**
- Task S-003.4 (Integrar Modal en ManageEventPage) - Necesita el componente

**Can Run In Parallel:**
- Task S-003.5 (Mostrar en EventDetail) - Son independientes

